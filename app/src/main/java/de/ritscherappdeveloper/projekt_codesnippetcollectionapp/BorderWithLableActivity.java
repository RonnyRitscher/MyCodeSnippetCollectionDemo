package de.ritscherappdeveloper.projekt_codesnippetcollectionapp;

import android.support.v7.app.AppCompatActivity;
import android.os.Bundle;

/**
 * Border with Lable Demo
 * 1. erstellen des "Shape" unter "drawable"
 * 2. GUI - Relative-Layout inkl. EditText(Usereingabe) und TextView(LableText) in der .xml erstellen
 * 3.
 */

public class BorderWithLableActivity extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_border_with_lable);


    }
}
